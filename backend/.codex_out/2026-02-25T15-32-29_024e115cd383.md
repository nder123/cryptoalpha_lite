Короткий план проверки `/api/health`:

1. Проверить базовый ответ:
```bash
curl -i http://localhost:8000/api/health
```
Ожидание: HTTP `200 OK`.

2. Проверить, что ответ валидный JSON:
```bash
curl -fsS http://localhost:8000/api/health | jq .
```
Ожидание: команда завершается без ошибки, JSON корректно парсится.

3. Проверить автопроверку/стабильность (несколько вызовов подряд):
```bash
for i in {1..5}; do curl -fsS -o /dev/null -w "%{http_code}\n" http://localhost:8000/api/health; sleep 2; done
```
Ожидание: все 5 ответов — `200`.

4. При сбое сразу посмотреть логи API:
```bash
tail -n 100 /home/ander/CascadeProjects/cryptoalpha_lite/backend/.uvicorn.log
```
Ожидание: понять причину, если статус не `200` или есть таймаут.

Unified diff: не применимо, так как по вашему требованию изменения файлов не выполнялись.  
Список правок по файлам/строкам: правок нет.

RESULT:
- Прочитан файл [docs/scratchpad.md](/home/ander/CascadeProjects/cryptoalpha_lite/docs/scratchpad.md).
- Взята и выполнена именно задача: `тест codex: напиши короткий план как проверить /api/health`.
- Выдан короткий практический план проверки без изменений в репозитории.