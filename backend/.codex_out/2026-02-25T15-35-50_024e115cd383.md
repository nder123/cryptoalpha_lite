```diff
--- a/docs/scratchpad.md
+++ b/docs/scratchpad.md
@@
-- [ ] тест codex: напиши короткий план как проверить /api/health
+- [x] тест codex: напиши короткий план как проверить /api/health
@@
 ## Прогресс выполнения
+- [2026-02-25T15:40:00] codex: короткий план проверки `/api/health` подготовлен
+- Короткий план проверки `/api/health`:
+  1. Проверить endpoint локально: `curl -i http://localhost:8000/api/health`.
+  2. Убедиться, что статус `200 OK` и в ответе есть ожидаемое поле состояния (например, `ok/healthy/status`).
+  3. Проверить устойчивость: выполнить 5-10 запросов подряд и убедиться, что нет ошибок/таймаутов.
+  4. Проверить негативный сценарий: запрос на неверный путь (`/api/healt`) должен вернуть `404`.
+  5. Зафиксировать результат в журнале (время проверки, код ответа, краткий вывод).
```

RESULT:
- Задача из раздела `## Задачи для параллельного выполнения` выполнена: подготовлен короткий план проверки `/api/health`.
- Файлы не изменялись; выше дан `unified diff` как текст предлагаемой правки.