Под задачу `тест notify: пинг` предлагаю добавить unit-тесты для `backend/scripts/monitor_scratchpad_tasks.py::_notify` (проверка пинга через `notify-send` и graceful fallback).

```diff
diff --git a/backend/tests/test_monitor_scratchpad_tasks.py b/backend/tests/test_monitor_scratchpad_tasks.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/backend/tests/test_monitor_scratchpad_tasks.py
@@ -0,0 +1,54 @@
+from __future__ import annotations
+
+import importlib.util
+from pathlib import Path
+from typing import Any
+
+
+BACKEND_ROOT = Path(__file__).resolve().parents[1]
+SCRIPT_PATH = BACKEND_ROOT / "scripts" / "monitor_scratchpad_tasks.py"
+
+spec = importlib.util.spec_from_file_location("monitor_scratchpad_tasks", SCRIPT_PATH)
+assert spec is not None and spec.loader is not None
+monitor_scratchpad_tasks = importlib.util.module_from_spec(spec)
+spec.loader.exec_module(monitor_scratchpad_tasks)
+
+
+def test_notify_ping_calls_notify_send(monkeypatch) -> None:
+    calls: list[tuple[tuple[Any, ...], dict[str, Any]]] = []
+
+    def _fake_run(*args: Any, **kwargs: Any) -> None:
+        calls.append((args, kwargs))
+
+    monkeypatch.setattr(monitor_scratchpad_tasks.subprocess, "run", _fake_run)
+
+    monitor_scratchpad_tasks._notify("ping", "ping")
+
+    assert len(calls) == 1
+    args, kwargs = calls[0]
+    assert args[0] == ["notify-send", "--app-name", "CryptoAlpha", "ping", "ping"]
+    assert kwargs["check"] is False
+    assert kwargs["stdout"] is monitor_scratchpad_tasks.subprocess.DEVNULL
+    assert kwargs["stderr"] is monitor_scratchpad_tasks.subprocess.DEVNULL
+
+
+def test_notify_ping_ignores_missing_notify_send(monkeypatch) -> None:
+    def _missing_binary(*args: Any, **kwargs: Any) -> None:
+        raise FileNotFoundError
+
+    monkeypatch.setattr(monitor_scratchpad_tasks.subprocess, "run", _missing_binary)
+
+    monitor_scratchpad_tasks._notify("ping", "ping")
```

Проверки не запускал, так как работаю в режиме без изменений файлов.

RESULT:
- Задача `тест notify: пинг` оформлена как точечный `unified diff`: добавлен новый файл тестов `backend/tests/test_monitor_scratchpad_tasks.py` с двумя тестами на `_notify` (`ping`-вызов и обработка отсутствия `notify-send`).